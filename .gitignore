# Secrets — never commit
.env
*.env
!.env.example

# Dependencies
node_modules/

# Build output
server/public/

# SQLite data (shouldn't be in the repo anyway, but just in case of local runs)
*.db
*.db-shm
*.db-wal
/data/

# Editor/OS cruft
.DS_Store
.vscode/
*.log
